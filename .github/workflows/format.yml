name: Format

on:
  merge_group:
  pull_request:
  push:
    branches:
      - main

jobs:
  check-changed:
    name: Check Changed Files (Format)
    runs-on: ubuntu-24.04
    outputs:
      any_changed: ${{ steps.check.outputs.any_changed || steps.default.outputs.any_changed }}
    steps:
      - uses: actions/checkout@v4

      - name: Check relevant files
        id: check
        if: ${{ github.event_name == 'pull_request' }}
        uses: tj-actions/changed-files@v47.0.1
        with:
          files: |
            include/**
            src/**
            tests/**
            python/**

      - name: Default to run on merge queue and push
        id: default
        if: ${{ github.event_name != 'pull_request' }}
        run: echo "any_changed=true" >> "$GITHUB_OUTPUT"

  check-format:
    name: Check Format
    needs: [check-changed]
    if: ${{ !cancelled() && needs.check-changed.outputs.any_changed == 'true' }}
    runs-on: "ubuntu-24.04"
    steps:
      - uses: actions/checkout@v4

      - name: Setup cmake
        uses: lukka/get-cmake@latest

      - name: Install Ninja
        run: sudo apt install ninja-build

      - name: Configure
        run: |
          script/configure

      - name: Check format
        run: |
          ninja -C build format
          diff=$(git diff)
          echo -n "$diff"
          test "$(echo -n "$diff" | wc -l) -eq 0"

  format-check-passed:
    name: Format Check Passed
    needs: [check-changed, check-format]
    if: always()
    runs-on: ubuntu-24.04
    steps:
      - name: All checks passed
        run: |
          if [[ "${{ contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') }}" == "true" ]]; then
            exit 1
          fi
